# ============================================
# GIIP Full-Stack Application Configuration
# ============================================
# Copy this file to .env and update the values
# IMPORTANT: Never commit .env to version control
# ============================================

# ============================================
# Database Configuration
# ============================================
# PostgreSQL database settings
DB_NAME=giip_db
DB_USER=giip_user
DB_PASSWORD=your_secure_password_here_change_in_production
DB_HOST=localhost
DB_PORT=5432

# For Docker: Use DB_HOST=db
# For local development: Use DB_HOST=localhost

# ============================================
# JWT Authentication Configuration
# ============================================
# SECURITY: Generate a strong random secret (minimum 256 bits)
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_minimum_256_bits_change_this_in_production
JWT_EXPIRES_IN=1h

# ============================================
# Frontend Configuration
# ============================================
# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost

# For production, set to your actual domain:
# FRONTEND_URL=https://yourdomain.com

# ============================================
# Node Environment
# ============================================
# Options: development, production, test
NODE_ENV=development
PORT=3000

# ============================================
# Rate Limiting Configuration (Optional)
# ============================================
# Time window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window for general API endpoints
RATE_LIMIT_MAX_REQUESTS=100

# Maximum login attempts per window
LOGIN_RATE_LIMIT_MAX=5

# ============================================
# Docker-Specific Settings
# ============================================
# When running with docker-compose, these are automatically set:
# - DB_HOST=db (service name)
# - NODE_ENV=production
# - PORT=3000
